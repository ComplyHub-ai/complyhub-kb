# Archive — g04-nav-program/ (workspace-root, pre-KB working documents)

**Archived:** 17 September 2026, before deleting the originals from
`C:\Users\brian\complyhubworkspace\g04-nav-program\`.

**Why:** this folder was the ungoverned, workspace-root living-doc set for the
Navigation Consolidation Initiative (G04) before it was folded into
`rto-compass-hub/docs/kb/` as a governed sibling initiative
(`docs/kb/reference/g04-navigation-consolidation-plan.md` and its Phase 0-4
structure — see PR #1208, Decision D-025). This folder was never under git
version control, so this is the only durable copy of its full narrative detail
— the KB migration condensed the locked decisions, evidence, and status into
KB-shaped docs, but did not carry over the full investigative narrative (the
per-issue Decision log's exact reasoning, independent adversarial-review
findings, rejected-alternative discussion, etc.).

**Authoritative going forward:** `rto-compass-hub/docs/kb/reference/g04-navigation-consolidation-plan.md`
and its nested phase/packet docs. These archived files are historical
reference only — do not treat them as current status.

## Files

- `README.md`, `source-precedence.md` — the folder's own index/precedence
  rules (see PR #1208's KB docs for the current equivalent).
- `g04-route-contract-foundation.md` — Phase 0/1 decisions and PR sequence for
  the route-contract prerequisite (now `docs/kb/.../phase-2-route-contract-foundation/`).
- `g04b-nav-source-recon.md` — the original 14-issue recon and full
  per-issue decision log (now condensed into
  `docs/kb/.../phase-1-navigation-source-consolidation/`).
- `g04b-nav-optimization-plan.md` — PR-sequencing/SOLID-check wrapper (now
  condensed into `docs/kb/.../phase-1-.../` and `phase-3-catalog-rollout-completion/`).
- `lint-playwright-program.md` — the wider G00–G-LINT program ledger this
  initiative grew out of. **Not fully superseded by the G04 KB migration** —
  most of this file's scope belongs to the separate ComplyHub Quality Program
  (`docs/quality/`), which was out of scope for the G04 KB fold-in. Kept here
  for the same reason; if the Quality Program's own status ledger is ever
  retired or migrated, this copy remains the historical record.

**Note:** `lint-playwright-program.md` was left in place at its original
workspace-root location (not deleted) because `docs/quality/README.md` still
names it as the Quality Program's own operator-local ledger. Only the other
five files (G04-specific) were deleted from the workspace root after this
archive was made.
